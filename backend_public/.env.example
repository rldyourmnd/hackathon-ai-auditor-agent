# Server
APP_HOST=0.0.0.0
APP_PORT=8080
APP_DEBUG=true
APP_SECRET=change_me_super_secret
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Database (used by app)
# Matches infra/docker-compose.yml service `db` defaults
DATABASE_URL=postgresql+psycopg2://curestry:secure_password@db:5432/curestry

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT=http://localhost:8080/auth/google/callback
JWT_ISSUER=auditor.public
JWT_EXPIRE_MINUTES=120

# Internal API (pipeline) proxy base
INTERNAL_API_BASE=http://localhost:8000
HTTP_TIMEOUT_SECONDS=30
